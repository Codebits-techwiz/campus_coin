# 01 – Build Status

> **WORKING DRAFT for the team, not for submission.**
> Audited against actual source code on 2026-09-24. Every claim links to a file + route.

---

## Module-by-Module Build Status Table

| # | Module | Routes | Models / Services | Status | Evidence | Known Defects |
|---|--------|--------|-------------------|--------|----------|---------------|
| 1 | **Auth** | `POST /api/auth/register` · `POST /api/auth/login` · `POST /api/auth/admin-login` · `POST /api/auth/logout` · `POST /api/auth/forgot-password` · `POST /api/auth/reset-password` | `User.js` · `authService.js` · `authController.js` · `authRoutes.js` | ✅ Complete | `authService.js:9-34` — separate `loginUser` (rejects admin) and `adminLoginUser` (rejects student). Reset token: `crypto.randomBytes(32)` hashed SHA-256, 30-min TTL (`authService.js:40-46`). Cookie is `httpOnly` (`authController.js:7`). | Rate limiter on auth routes only; non-auth API has no global rate limiter. `emailService.js:9` calls `nodemailer.parseTransport` (non-existent API) — should be `createTransport`; password-reset email will fail silently in production. |
| 2 | **User Profile** | `GET /api/users/me` · `PUT /api/users/me` | `User.js` · `userService.js` · `userRoutes.js` | ✅ Complete | `User.js:35-50` — `academicYear`, `monthlyAllowanceBaseline`, `monthlySavingsGoal`, `currency` all present. `userService.js` handles profile update. | Profile update does not enforce password re-confirmation before changing sensitive fields. |
| 3 | **Categories** | `GET /api/categories` · `POST /api/categories` · `PUT /api/categories/:id` · `DELETE /api/categories/:id` | `Category.js` · `categoryService.js` · `categoryRoutes.js` | ✅ Complete | `categoryService.js:26-49` creates custom with owner. `categoryService.js:56-77` blocks student from editing `isDefault`. `seed.js:104-117` seeds all 12 SRS default categories (5 income + 7 expense). | None identified. |
| 4 | **Transactions** | `GET/POST /api/transactions` · `GET/PUT/DELETE /api/transactions/:id` · `POST /api/transactions/import-csv/preview` · `POST /api/transactions/import-csv/confirm` · `POST /api/transactions/scan-receipt` | `Transaction.js` · `transactionService.js` · `csvImportService.js` · `transactionRoutes.js` | ✅ Complete | Soft-delete: `transactionService.js:176`. Pagination + filters: `transactionService.js:38-81`. Ownership enforced every mutating call. CSV formula injection sanitization: `csvImportService.js:10-17`. | None on transaction layer itself; see Budget module for runtime crash from `require()` in ESM. |
| 5 | **Recurring Rules** | `GET/POST /api/recurring` · `PUT/DELETE /api/recurring/:id` | `RecurringRule.js` · `recurringService.js` · `recurringCron.js` · `recurringRoutes.js` | ✅ Complete | CRUD: `recurringService.js:12-75`. Cron fires midnight daily (`recurringCron.js:10`). Creates transaction + advances `nextRunDate` (`recurringService.js:92-113`). | Cron errors only console-logged — no alerting. No test for month-boundary edge (e.g. Jan 31 → skips Feb). |
| 6 | **Budgets** | `GET /api/budgets` · `POST /api/budgets` · `DELETE /api/budgets/:id` | `Budget.js` · `budgetService.js` · `budgetRoutes.js` | ⚠️ Faulty | Budget CRUD and alert engine present. Unique index `{ user, category, month }`: `Budget.js:42`. Alert at 80%/100% with de-duplication: `budgetService.js:139-180`. | **Critical Bug:** `budgetService.js:34` and `:117` use `new (require('mongoose').Types.ObjectId)(userId)` inside an ES Module — `require` is not defined in ESM; runtime crash. Aggregation in `getBudgetsForMonth` and `checkAndTriggerBudgetAlert` will fail. Fix: replace with `mongoose.Types.ObjectId` via top-level import. |
| 7 | **Dashboard** | `GET /api/dashboard/summary` | `dashboardService.js` · `dashboardController.js` · `dashboardRoutes.js` | ⚠️ Partial | Greeting, month totals, top category, budget-vs-actual all real: `dashboardService.js:96-110`. | `topTips` at `dashboardService.js:112-125` returns **hardcoded static placeholder strings** — the real tips engine is never called here. |
| 8 | **Reports** | `GET /api/reports/category-breakdown` · `GET /api/reports/trend-6months` · `GET /api/reports/daily-weekly` · `GET /api/reports/export-pdf` · `POST /api/reports/share-email` | `reportService.js` · `pdfService.js` · `emailService.js` · `reportController.js` | ✅ Complete | Category breakdown with date+type filter: `reportService.js:11-66`. 6-month trend: `reportService.js:72-116`. Daily: `reportService.js:121-163`. PDF: `pdfService.js`. Email: `emailService.js:23-95`. | `emailService.js:9` — `nodemailer.parseTransport` does not exist; should be `createTransport`. Weekly aggregation not implemented (only daily groups returned from daily-weekly endpoint). |
| 9 | **AI – Category Prediction** | `POST /api/ai/predict-category` · `POST /api/ai/feedback` | `aiCategorizerService.js` · `CategoryCorrection.js` · `aiRoutes.js` | ✅ Complete | 3-tier priority: user corrections → Naive Bayes (`natural` npm) → keyword fallback: `aiCategorizerService.js:46-134`. Feedback stored via upsert + count increment: `aiCategorizerService.js:156-163`. | Naive Bayes classifier rebuilt from scratch on every request — no in-memory cache; degrades under load. |
| 10 | **AI – Monthly Insights** | `GET /api/ai/monthly-insights` | `aiInsightsService.js` · `Insight.js` | ✅ Complete | Aggregate facts → Gemini 1.5 Flash (if `GEMINI_API_KEY`) → template fallback: `aiInsightsService.js:108-156`. DB-cached per user+month. Disclaimer appended always: `aiInsightsService.js:26,169`. | No rate limiting on endpoint — could trigger many Gemini calls. Cached insight cannot be refreshed without manual DB deletion. |
| 11 | **AI – Saving Tips** | `GET /api/ai/saving-tips` · `POST /api/ai/saving-tips/:id/pin` · `POST /api/ai/saving-tips/:id/dismiss` | `tipsEngineService.js` · `Tip.js` · `TipAction.js` · `TipTemplate.js` | ✅ Complete | 3 deterministic rules (spike >120%, budget ≥80%, goal at risk): `tipsEngineService.js:90-175`. Admin template override: lines 25-34. Pin/dismiss: lines 208-222. | `evaluateSavingRules` line 140 uses `forEach(async ...)` — async callbacks inside `forEach` are **not awaited**; template resolution is fire-and-forget. Use `for...of` instead. |
| 12 | **AI – Forecast** | `GET /api/ai/forecast` | `forecastService.js` | ✅ Complete | 3-month moving average projection: `forecastService.js:11-69`. Confidence label based on data-point count. | Response claims `method: "3_month_moving_average_linear_regression"` but code computes a simple average, not true linear regression. |
| 13 | **Bookmarks** | `GET/POST /api/bookmarks` · `PATCH/DELETE /api/bookmarks/:id` | `Bookmark.js` · `bookmarkService.js` · `bookmarkRoutes.js` | ✅ Complete | Supports `tip` and `insight` refTypes. Ownership verified before create: `bookmarkService.js:44`. Unique index prevents duplicates. | No PDF export specifically for bookmarks (K2 partial). |
| 14 | **Notifications** | `GET /api/notifications` · `PATCH /api/notifications/:id/read` | `Notification.js` · `notificationService.js` · `notificationRoutes.js` | ✅ Complete | Budget alerts created by `budgetService.checkAndTriggerBudgetAlert`. Anomaly alerts created by `anomalyService.js`. De-duplication per month. | Notification list has no pagination. |
| 15 | **Admin** | `GET /api/admin/stats` · `GET/PATCH/POST /api/admin/users/:id/*` · `POST/PUT/DELETE /api/admin/categories/:id` · `GET/POST/PUT/PATCH/DELETE /api/admin/announcements` · `GET/POST/PUT/DELETE /api/admin/tip-templates` | `adminService.js` · `adminController.js` · `adminRoutes.js` | ✅ Complete | Role guard on all routes: `adminRoutes.js:11-18`. Stats exclude individual transactions. Category delete blocks if active transactions exist unless `reassignTo` provided: `adminService.js:95-118`. | Admin stats volume returned in raw cents (no currency conversion). |
| 16 | **Activity Log (S1)** | `GET /api/activity/recent` | `ActivityLog.js` · `activityService.js` · `activityController.js` · `activityRoutes.js` | ✅ Complete | Over-fetch 50 logs → dedup → return 10 unique: `activityService.js:34-49`. Ownership enforced on tx lookup. Fire-and-forget write prevents blocking request path. | None. |
| 17 | **Receipt OCR** | `POST /api/transactions/scan-receipt` | `receiptOcrService.js` | ⚠️ Partial | Regex-based amount extraction + heuristic merchant from UTF-8 text: `receiptOcrService.js:14-25`. Category predicted via AI engine. | Binary images (JPG/PNG/PDF) return hardcoded fallback values (`amount: 15.0`, `merchant: "Campus Cafe & Bookstore"`). Not real OCR. SRS does not mandate this feature. |
| 18 | **Announcements (public)** | `GET /api/announcements` | `Announcement.js` · `announcementController.js` · `announcementRoutes.js` | ✅ Complete | Returns `isActive: true` only for students. Admins see all via admin route. | None. |
| 19 | **Transaction Templates** | `GET/POST/PUT/DELETE /api/templates` | `TransactionTemplate.js` · `transactionTemplateController.js` · `transactionTemplateRoutes.js` | ✅ Complete | Extra feature beyond SRS, noted in `server.js:76`. | None — correctly marked extra feature. |
| 20 | **Health Check** | `GET /api/health` | `server.js:52-59` | ✅ Complete | Returns `status`, app name, version, timestamp. | No DB connectivity check in health response. |

---

## Summary

| Status | Count |
|--------|-------|
| ✅ Complete | 16 |
| ⚠️ Partial | 2 |
| ⚠️ Faulty | 1 (Budget — `require()` in ESM) |
| ❌ Missing | 0 |

*See `02-srs-compliance.md` for per-SRS-ID mapping and `05-remaining-work.md` for the prioritised fix backlog.*
